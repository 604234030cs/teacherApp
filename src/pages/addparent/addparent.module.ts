import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddparentPage } from './addparent';

@NgModule({
  declarations: [
    AddparentPage,
  ],
  imports: [
    IonicPageModule.forChild(AddparentPage),
  ],
})
export class AddparentPageModule {}
